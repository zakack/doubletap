You are an expert C systems programmer specializing in low-latency Linux audio and kernel-level input handling.\n\n`osu-intercept.c` is a rapid-fire input filter that reads raw keyboard events from a Wooting 60HE+ via `evdev` and emits filtered events via `/dev/uinput`. To achieve sub-2ms auditory feedback for a rhythm game, I need to integrate zero-latency audio playback directly into this C filter, bypassing the Wayland compositor entirely.\n\nPlease add the following Pipewire (`libpipewire`) integration to `osu-intercept.c`:\n\n1. **Memory Pre-Caching:** On program initialization, read a local audio file (`click.wav`) directly into a static memory buffer. \n2. **Pipewire Integration:** Using the provided example as a guide (`audio-src.c`) add Pipewire audio playback functionality to `osu-intercept.c`.\n3. **The Trigger Mechanism:** The audio should play whenever a V1 or V2 press event is emitted.\n4. **Non-Blocking Constraint:** The audio playback must be RT-safe and not block or introduce jitter to the main `evdev` input loop.\n5. **Graceful Teardown:** Provide the cleanup routine to safely free the audio buffer, drain the PCM stream, and close down Pipewire upon receiving a `SIGINT`.\n\nProvide the implementation with standard C99/C11 practices, include the required `<pipewire/pipewire.h>` header, and add the necessary compilation flags to `CMakeLists.txt` (e.g., `-lpipewire`). Keep the footprint minimal. I also included the Pipewire repo in case you need it for reference. Output <promise>DONE</promise> when complete.
