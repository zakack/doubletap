# Let members of the `input` group open /dev/uinput, so osu-interceptd can
# run as an unprivileged systemd user service. static_node makes the node
# exist at boot; opening it autoloads the uinput module.
KERNEL=="uinput", MODE="0660", GROUP="input", OPTIONS+="static_node=uinput"
